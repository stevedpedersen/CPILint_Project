---
common:
  pipelineType: btp
  debug: true
 
  btp:
    packageId: SPDevPackage2
    proxyNames: 
      - My_API_Proxy_V1

  staticAnalysis:
    enabled: false # enable manually via Jenkins params
    type: cpilint
    ruleset: "default"
    iFlows: "" # set via build params if desired
